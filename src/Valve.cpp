#include <Arduino.h>
#include "Valve.h"
Valve::Valve() {
    //_state = UNKNOWN;

/*
    public open() {
        // Activate a solenoid or send a command via MQTT, etc
        //_desiredState = OPEN;
        //setGraphic (OPENING);
    }
*/

}

void Valve::close() {
    ;
}